<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="project.blog.community.project.mapper.WeatherMapper">
<select id="getCoord" resultType="map">
SELECT
    nx,ny
    FROM tbl_weather
    WHERE area1 = #{area1}
    AND area2 LIKE CONCAT('%', #{area2}, '%')
    limit 0, 1
</select>
    
    
</mapper>