가위바위보

시작
베팅 금액 입력 후 submit
	int bet = 12000;
	내 포인트에서 베팅 금액만큼 차감

가위 바위 보 중 하나 선택 후 submit
	enum type rps = RPS.PAPER;

컴퓨터 랜덤 결정
	int a = Math.random(2);
	if ( a == 0 ) comRps = RPS.ROCK;	
	else if ( a == 1 ) comRps = RPS.PAPER;
	else comRps = RPS.SCISSORS;

승부 결정
	if (rps == RPS.ROCK) {
		
	}

head 태그
<%@ include file="../include/static-head.jsp" %>
<link rel="stylesheet" href="/assets/css/페이지디자인.css" >
<link rel="stylesheet" href="/assets/css/snb.css" >

body 태그 
<%@ include file="../include/snb.jsp" %>






