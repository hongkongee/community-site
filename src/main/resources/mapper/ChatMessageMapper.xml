<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="project.blog.community.otochat.repository.ChatMessageMapper">

    <insert id="save">
        INSERT INTO chat_message

    </insert>

    <select id="findByRoomNumber" resultType="chat">
        SELECT * FROM chat_message

    </select>

    <select id="findTopByRoomNumberOrderByCreatedTimeDesc" resultType="chat">
        SELECT * FROM chat_message

    </select>

    <select id="findAllByRoomNumber" resultType="chat">
        SELECT * FROM chat_message

    </select>

</mapper>