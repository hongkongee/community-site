<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="project.blog.community.project.mapper.BoardMapper">

    <select id="save">
        INSERT INTO tbl_board
        (title, content, writer, category)
        VALUES (#{title}, #{content}, #{writer}, #{category})
    </select>

    <select id="findAll" resultType="board">
        SELECT * FROM tbl_board
        ORDER BY bno DESC
    </select>

    <select id="findOne" resultType="board">
        SELECT * FROM tbl_board
        WHERE bno = #{bno}
    </select>


    <update id="updateViewCount">
        UPDATE tbl_board
        SET view_count = view_count + 1
        WHERE bno = #{bno}
    </update>

    <update id="updateReportCount">
        UPDATE tbl_board
        SET report_count = report_count + 1
        WHERE bno = #{bno}
    </update>

    <select id="findMyList" resultType="board">
        SELECT * FROM tbl_board
        WHERE writer = #{myAccount}
        ORDER BY bno DESC
    </select>




</mapper>